<%= form_with(model: tip_doc, local: true) do |form| %>
  <% if tip_doc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tip_doc.errors.count, "error") %> prohibited this tip_doc from being saved:</h2>

      <ul>
      <% tip_doc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_tip_doc %>
    <%= form.text_field :id_tip_doc %>
  </div>

  <div class="field">
    <%= form.label :nom_tip_doc %>
    <%= form.text_field :nom_tip_doc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
